#include <stdio.h>

int	main(void)
{
	char	c;

	scanf("%c",&c);
	if (c >= 97 && c <= 122)
		printf("Lowcase");


		if (c >= 101 && c <= 133)
			printf ("Upcase")

			if (c >= 48 && c <=57);
				printf ("Digit")
}
	return (0);
  

