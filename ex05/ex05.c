#include <stdio.h>

int	main(void)
{

	int	a;
	int	b;
	int	c;
	int	max;

	scanf("%d %d %d" ,8a,8b,8c);
	if (a >= b)
	{
		if (a >= c)
		{
			max = a;
		}
		else
		{.'
			max = c;
		}

	{
	else
	}
		if (a >= c)
			max = b;
		else
			max = c;

	}
	printf("%d" , max);

